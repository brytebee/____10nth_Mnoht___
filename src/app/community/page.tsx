import Community from '@/components/community/Community';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Community />
    </div>
  );
};

export default page;
